# CenturyBridge 项目计划与工作量评估

> 目标：让已停更的老版本 Minecraft 模组（1.13+，首期聚焦 1.20.1）在当前最新版本上与新模组共同运行。
> 心智模型：Wine/Proton —— 在新版 vanilla 上重实现老版本的 API 表面 + per-mod quirks 数据库。
> 起草日期：2026-08-15

---

## 1. 范围决策

### 首期走廊（P0–P2 全部围绕它）
- **源版本：Fabric 1.20.1**（停更模组语料最大的单一版本）
- **目标版本：当前最新 Fabric 版本**（1.21.x / 26.x drop）

选 Fabric 优先的决定性理由：Fabric 生产环境的运行时命名空间**就是 Intermediary**，老模组的字节码引用和新版运行时天然同名空间。凡是跨版本存活的符号，**零重映射直接可用**；工作量全部集中在"消失/变形的符号"的桥接上。Intermediary + Matcher 还免费提供了跨版本身份图谱（哪个方法是哪个方法的继承者）。

### 明确的非目标（首期）
- Forge/NeoForge 源模组（无 Intermediary，且 1.20.1→1.21 跨越 NeoForge 分裂，工作量约等于再造一个项目，列为 P4 独立决策）
- 自定义渲染管线重的模组（1.21.x 渲染层持续重构，明确标记为"不支持"而非"尽力支持"）
- 让老整合包获得新版本内容 —— 本项目方向相反：老模组进新世界

---

## 2. 架构总览（五个子系统）

| # | 子系统 | 职责 | 自动化程度 |
|---|--------|------|-----------|
| S1 | 加载壳 | 发现 `bridged-mods/` 内的老 jar，经变换后并入 Fabric 加载流程 | 全自动 |
| S2 | 桥接注入引擎 | 向当前版本 vanilla 类注入老签名的合成桥接方法；为已删除的类生成同名 facade。**单一类空间，保持类身份唯一**（新老模组互通的前提，排除 classloader 隔离方案） | 引擎自动，shim 内容人工 |
| S3 | Mixin 管线 | 老模组 Mixin 的重定向（基于 Intermediary diff 自动 + 人工规则表）、apply 失败拦截、分级降级 | 半自动 |
| S4 | 数据/资产翻译 | pack format 升级、配方/战利品表/模型 JSON 迁移、NBT ↔ Data Components 双向桥 | 基本全自动 |
| S5 | Quirks 数据库 | per-mod 补丁条目 + 社区贡献流程 + skip 日志遥测回流 | 人工/众包 |

### 降级阶梯（S3 核心设计）
1. Mixin 静态分类：**装饰性**（渲染/提示类，可安全跳过）vs **承重**（加接口、`@Shadow` 写入被模组自身读取的状态 → 跳过会延迟崩溃）。
2. 装饰性 Mixin apply 失败 → warn + skip，模组降级运行。
3. 承重 Mixin 失败 → 整个模组拒载或标记"已降级、可能不稳定"。
4. 运行时兜底：事件分发边界捕获降级模组异常，禁用其处理器。**红线：服务端改世界状态的路径禁止吞异常**（防静默存档损坏），只对客户端/渲染/装饰路径生效。
5. 所有 skip 日志汇总 → quirks 数据库优先级列表（用户替我们投票哪个注入点最值得人工写规则）。

---

## 3. 分阶段计划与工作量估算

估算基准：一名熟悉 ASM/Mixin/Fabric 内部的全职开发者。“周”= 人周。

### P0 · 可行性量化 —— 可链接性分析器（2–3 周）

在写任何 shim 之前，用数据回答"大部分能兼容"到底是百分之多少。

**抽样框（关键，比语料量重要）**：不按下载量排序取——下载榜头部是优化类模组（Sodium/Lithium 系），Mixin 饱和、且桥接价值为零（新版本有自己的优化模组，没人需要在新版跑老 Sodium）。正确的框：

- 只取**死亡模组**：最后支持版本 == 源版本（仍在更新的模组不需要桥）
- 按类别分层抽样（装饰/食物/工具/玩法/科技/魔法…），优化与渲染类单独统计、排除出覆盖率 KPI
- 每源版本 500–1000 个死模组即可统计稳定（扫描是静态分析，量本身不贵，框才是重点）
- **依赖图分析找 keystone 库**（Cloth Config、owo-lib、architectury 等）：桥好一个库救活整片依赖者，shim 优先级表必须按扇出加权

| 任务 | 工作量 |
|------|--------|
| Modrinth 语料抓取器（按上述抽样框，含依赖闭包） | 3–4 天 |
| ASM 扫描器：常量池 vanilla 符号引用 + Mixin 注解解析（目标、@At 规格、局部变量捕获） | 1 周 |
| Intermediary 两版 diff + 符号存活率统计 | 2–3 天 |
| 承重/装饰 Mixin 分类启发式 | 2–3 天 |
| 依赖图 + keystone 库扇出分析 | 2 天 |
| 产出报告：链接率曲线 + 按扇出加权的符号频率表（即 shim 优先级清单） | 2 天 |

**产出三个数**：语料中 X% 干净链接 / Y% 可降级运行 / Z% 拒载。

#### ✅ P0 试点已完成（2026-08-15，1.20.1 → 26.2，240 个死亡内容模组）

| 指标 | 结果 |
|------|------|
| A 直通 + B 签名桥（自动可跑） | **19.2%** |
| A+B+C（架构可覆盖，含 facade shim + 降级） | **77.5%** |
| D 承重 Mixin 死亡（quirks/LLM 管线领域） | 22.1% |
| 承重 Mixin 完整存活率 | 75.4%（609/2479 损坏） |

**G0 数据门判定：77.5% ≥ 50% → 继续 P1。**

关键发现：受损符号高度集中在计划预测的墙上——物品组件（`ItemStack.getTag`、`CompoundTag.*`）、渲染/GUI 重写（`GuiGraphics` 整类消失、`RenderType.cutout`、`VertexConsumer.endVertex`）、注册表面内部类（`Item$Properties` 波及 117/240、`BlockBehaviour$Properties` 89、`CreativeModeTab$Builder` 81）。注册表面是十几个 facade 救回大半个 decoration/equipment 类别的超高杠杆点。完整报告：`data/reports/P0-report-1.20.1-to-26.2.md`。

已知方法学下界偏差：1.21.11（最后一个 intermediary）之后的 Mojmap 改名会被误计为 L3，真实数字应略好于上表；语料中 26.x 段需要一次 Matcher 比对来消除（后续任务）。

技术附注（构建中发现）：① 26.1 起 Minecraft 不再混淆、Fabric 弃用 intermediary，新端符号直接扫 client.jar；② Mixin 注解字符串不在发布时重映射而靠 refmap 运行时翻译，扫描器必须同样处理；③ 分析器已实现"1.20.1 →(intermediary 稳定段)→ 1.21.11 →(ProGuard)→ Mojmap"链式解析——分段链架构的第一次实战验证，类存活率因此从 83.3% 修正到 90.2%。

#### ✅ 死亡归因已完成（逐边界检查点链，`data/reports/death-attribution.md`）

21 条边界（22 个正式版含全部小版本）逐一检查：**9 条零伤亡**（实证"多数小版本边界为自动直通段"）；损伤集中在 1.20.4→1.20.5（组件墙）、1.21.1→1.21.2、1.21.4→1.21.5（渲染）、1.21.11→26.1 与 26.1.2→26.2（后 intermediary 时代，含改名盲区通胀）。每条边界的 kill list = 该分段的 shim 工单。26.x 起跨版本身份由我们自建（每相邻对一次 diff，写完冻结——避免"每源版本直连最新"的平方成本）。

#### ✅ 运行时 PoC 已完成（静态重定向器 `analyzer/retarget.py`）

**`crop-growth-modifier`（1.20.1 死亡模组，A 桶）经重定向后在 Fabric 26.2 服务端完整启动到 "Done"，零报错**——机制主链（intermediary→Mojmap 常量池改写 + refmap 改写 + fabric.mod.json 依赖修补）端到端打通。`block-swap`（B 桶）按预测止步于签名桥缺失，其静态验证给出精确 shim 工单（96/102 引用直通 + 4 个 CompoundTag L2 + 2 死符号）。

#### ✅ Java 引擎：Mixin triage 移植完成 + intermediary 时代全链贯通

新增 `Chain`（分段组合 + **边界归因**：每个损伤标注死于哪条边界）与 `MixinTriage`（完整移植 Python 版全部语义坑：refmap 限定键、`<init>` 描述符死类、隐式 accessor 推断；ASM AnnotationVisitor 原生嵌套注解）。`gen-chain` 一条命令生成 1.20.1→1.21.11 全部 **17 节分段**，5 条边界零 diff（纯直通实证），三堵墙符号量：1.20.5 组件墙 1369 方法死亡、1.21.2 墙 1658、1.21.5 渲染墙 1853。实测：blockswap 过 17 节链 → `MixinMinecraftServer` 自动摘除且归因精确至 **1.21.8→1.21.9**（class_3950 卒年查明）；`StateHolderAccess` 正确**保留**（其目标 1.21.11 仍存活，死于 26.x）——逐目标版本差异化 triage 即分段架构的核心收益。数据修正：CompoundTag getters 的 Optional 化发生在 26.x 窗口而非 1.21.5。运行时验证：crop 经链转换后在 Fabric **1.21.1** 服务端 "Done (4.238s)" 零错误（跨组件墙 + 1.21 墙）。

鲁棒性扫荡：全语料 270 jar 过 17 节链，**引擎零崩溃**；判定分布（@1.21.11）：direct 44 / partial 121 / degraded_partial 103 / degraded 2。**验证成色声明**：已证"能加载"（3 个版本真服）与"引擎不炸"（全语料）；"判定正确"仅两模组级锻炼（待 Java↔Python 同口径 parity diff）；"行为正确"零验证（待冒烟 harness / 人工游玩）。

**下一块：边境段（1.21.11→26.1）**——Java 侧需要：改名表进 Segment 数据（datagen 读 ProGuard + client jar 清单）、ClassRemapper + 注解/LDC 字符串 token 替换、kind 修复（含每版本库 kind 数据）、26.x 段 inventory diff 生成。完成后 core/ 全线贯通 1.20.1→26.2。

#### ✅ 纯 Java 引擎（core/）首节分段落地

`core/src/top/dext/centurybridge/`：TinyMappings（tiny v2 解析+描述符重映射）、SegmentGen（构建期分段生成器雏形）、Segment（冻结分段数据模型，gson）、JarProcessor（ASM 验证+fmj 修补+产物输出）、Main（CLI：gen-segment / convert）。依赖仅 ASM+gson（运行时零额外依赖）。**分段 1（1.20.1→1.20.2）已由 Java 生成并验证**：19 类 / 933 方法死亡、665 签名变形；crop/blockswap/addurdisc 三模组转换均 ok_direct，crop 转换产物在 Fabric 1.20.2 服务端 "Done (11.240s)" 零错误。验证了"intermediary 时代内部分段零改名"的架构判断。待移植（Python→Java）：Mixin triage、kind 修复（ASM 下退化为 visitMethodInsn 改 itf 标志）、边境段改名（ClassRemapper + 注解/LDC 字符串 token 替换）、shim 组装、墓碑桩生成。

#### ✅ P1 管线原型（cb/cb.py）已跑通端到端

`python cb/cb.py <cbmods> --deploy <mods>` 一条命令完成：版本门控 → Mixin triage（refmap 限定键匹配 + 构造器描述符内嵌死类检测，两个盲点已在验收中修复）→ 改名 → kind 修复 → 死 mixin 自动摘除（承重者带警告）→ 静态验证（减去 shim 覆盖面）→ 依赖检查 → 判定与报告（`recovered/report.md`）。判定策略按墓碑哲学：缺符号 = ok_partial 照常部署（惰性失败），缺依赖 = blocked（loader 解析级失败）。运行时桥从 `cb/shims/` 源目录 + manifest 自动组装编译。验收：4 个原始 jar 输入 → blockswap(ok_partial, 自动摘 2 mixin) + crop(ok_direct) 部署，服务器 "Done (0.328s)" 零错误；eternal_attributes/experienced_crops 正确 blocked 并给出可操作建议。最终形态即 3.5 节架构：mods/ 仅一个 CB jar，产物在 cbmods/recovered/，fabric.addMods 加载。

#### ✅ P1 首块 shim 已完成：B 桶模组 block-swap 成功上线 26.2

全链路验证："Done (0.736s)"，零模组相关错误。组件：
- **centurybridge-rt 运行时桥模组**（2KB，`data/runtime/cb-rt/`）：Mixin 向 26.2 `CompoundTag` 注入老签名重载（`getString/getBoolean/getCompound/getList/remove/method_10573`，全部一行委托到新 `*Or/*OrEmpty` API）——**桥接方法注入架构实证**：JVM 按描述符解析，老调用点零改动复活。26.x 不混淆时代无需 loom，javac 对 client.jar 直接编译。
- **Access widener**：`Identifier` 私有化构造器重新放开——访问级别 shim 类别实证。注意：不混淆时代 Fabric 运行时命名空间叫 `official`（AW 头必须写 `accessWidener v2 official`）。
- **类↔接口修复引擎**（`analyzer/bytecode_shim.py`）：第四个失效维度——DFU 7.0 将 `DataResult` 由类改接口，老 jar 的 Methodref 标签非法。修复全部等长、零偏移变动：① cp 标签翻转（10↔11，修静态调用）；② `invokevirtual`(3B) 原地换 `invokestatic`(3B) 指向自动合成的委托桩；③ `invokeinterface`(5B) 换 `invokestatic`+2×nop。委托桩类由 Python 直接生成字节码（无分支代码不需要 StackMapTable）。block-swap 实测：6 类翻标签、11 调用点重定向、6 桩合成。
- **降级摘除**：2 个真死 mixin（`StateHolderAccess` 目标字段被重构掉、`MixinMinecraftServer` 构造器签名变化）从配置摘除，模组降级运行。

分析范围修正：`com/mojang/serialization|datafixers`（DFU）、brigadier 等 Mojang 库命名空间此前未纳入存活分析，DFU 6→7 是重大断层——扫描器需将这些库并入检查范围。

实测暴露的新失败维度（进 P1 需求）：
- **访问级别变化**：`Identifier` 构造器 1.21 私有化 → `IllegalAccessError`。分析器需比对 access flags；通用解为 access widener 类 shim。
- **依赖时代问题**：老 fabric-api 伞形 id `fabric` → 新 `fabric-api`（已在重定向器修补）；keystone 库缺位（necronomicon/moonlight）需"库也过桥或装新版"；野生模组存在未声明依赖（cloth-config）。
- Mixin `@Accessor` 目标字段在 26.2 真死（StateHolder 内部重构）——摘除该 mixin 后可继续加载，降级阶梯路径实测可行。

### P1 · 核心运行时（累计 24–39 周；MVP 子集 12–16 周）

| 任务 | 工作量 | 说明 |
|------|--------|------|
| S1 加载壳、老模组嵌套加载 | 2–3 周 | Fabric Loader 集成较琐碎 |
| S2 桥接注入引擎（方法注入 + facade 生成框架） | 2–3 周 | 引擎本身，不含 shim 内容 |
| **Item Components 双向桥**（`getTag`/`setTag` 在组件系统上的模拟） | 4–6 周 | 最大单体墙，语义保真难 |
| 网络层桥（1.20.2 config phase + 1.20.5 payload 重构，含 Fabric API 网络模块变更） | 3–4 周 | |
| 数据驱动化桥（1.21 附魔转数据包等：代码注册 → 运行时生成数据条目） | 2–4 周 | 每个子系统 bespoke |
| 高频签名桥（按 P0 频率表逐个覆盖的长尾） | 4–8 周 | 数据驱动，可持续追加 |
| Fabric API 兼容层（老 API 模块签名 → 新模块） | 2–4 周 | |
| S3 Mixin 管线（Intermediary 重定向 + 降级阶梯 + apply 失败拦截） | 3–4 周 | |
| S4 资产/数据格式翻译（pack format、配方/模型 JSON） | 2–3 周 | 机械劳动为主 |

**里程碑 M1（MVP，≈3–4 个月）**：10 个精选纯内容模组（装饰/物品/配方类）在最新版可正常游玩。
达成路径：加载壳 + 注入引擎 + Item Components 桥 + 部分高频签名桥，网络/附魔桥可后置。

**里程碑 M2（公测，累计 ≈6–9 个月）**：P0 语料 50% 以上可运行（含降级），quirks 贡献流程开放。

### P2 · 覆盖率与基建（3–4 个月，与 P1 后半并行）

| 任务 | 工作量 |
|------|--------|
| CI 语料回归：无头服务器批量加载 N 个模组，自动检测崩溃/降级回归 | 3–4 周 |
| **注册表驱动冒烟测试 harness**：枚举模组注册的物品/方块/配方/实体，机械地放置/使用/合成并检查崩溃与基本行为——写一次覆盖所有内容模组，是 LLM 补丁管线的裁判 | +2–3 周 |
| 遥测（opt-in）+ skip 日志聚合管线 | 2 周 |
| Quirks 数据库格式 + 社区 PR 贡献流程 | 2 周 |
| **LLM 自动补丁管线**：skip 遥测 → LLM（廉价 token 批量）读旧/新方法体生成候选重定向或改写 → apply 检查 → 冒烟测试 → 绿灯自动入库、黄灯人审。生成成本趋近于零，瓶颈全在验证侧；触碰服务端世界状态的补丁一律强制人审 + 存档往返测试 | 2–3 周（管线本身） |
| 覆盖率打磨 | 由 LLM 管线承担大头，人工只处理黄灯队列 |

> LLM 管线的战略意义：它抬高的是**单 Mixin 修复率**，直接攻击"兼容率 = 各耦合点存活率乘积"里的指数本身。长期"quirks 社区运营"成本大部分转化为计算成本 + harness 维护，覆盖率天花板随 token 投入持续上移。

### P3 · 常态维护（持续成本）

| 项目 | 成本 |
|------|------|
| 每个 Minecraft drop（26.x 节奏下频率变高） | 1–3 周/次：Intermediary diff → shim 栈前移 → CI 回归 |
| 新增源版本（1.19.2 → 1.18.2 → 1.16.5，pivot 模型：目标永远只有当前版本） | 各约首条走廊 shim 成本的 30–50% |
| Quirks 社区运营 | 长期低强度 |

### P4 · 全跨度扩展：1.14 → 当前最新（M2 之后启动）

> 范围修正：Fabric 生态始于 1.14（1.13 只有 Rift，无官方 Intermediary，生态可忽略），故"1.13+"实际即 1.14+。Forge/NeoForge 轴已决定放弃，不做。

**架构前提（决定成本形态）**：shim 按相邻版本边界分段（ViaVersion 模型），数据上链式组合、运行时压平为每个源版本的直达表。由此：老分段写完永久冻结；每个新 drop 只新增一节头部分段，不触碰历史分段。

**节点粒度 = 每个正式版**（1.14–26.3 约 50+ 个节点，含全部小版本）。补丁号不承诺兼容性，源模组必须从其精确版本起链。但成本不等比增长：多数相邻小版本的 Intermediary diff 接近空集，由流水线自动生成直通段，人工抽查；需要人工写 shim 的仍约 15 节（下表的墙全部位于小版本边界——大版本概念在本项目中无意义，只有 diff 大小之分）。配套任务：**源版本自动识别**（`fabric.mod.json` 的模糊版本范围 + 符号指纹反推精确版本），约 1–2 周。

**边界难度地图**（首期走廊已覆盖后两堵墙）：

| 边界 | 难度 | 内容 |
|------|------|------|
| 1.14→1.15、1.15→1.16、1.18→1.19、1.19→1.20 | 中 | RenderLayer、动态注册表、GuiGraphics 等 |
| 1.16→1.17 | 大 | Java 17 + 渲染 core profile（自定义渲染器拒载） |
| 1.17→1.18 | 中大 | 世界高度 + worldgen 重写（worldgen 模组跨此边界拒载） |
| 1.20.2→1.20.5 | 最大 | 物品组件 + payload 网络（首期已含） |
| 1.21.x 序列 | 大 | 物品模型数据化、GPU 抽象层（首期已含） |

**工作量**：向后补人工分段（1.14←1.20.1 区间）+6–9 人月，一次性投资；自动直通段验证、50+ 节点 CI 矩阵、源版本识别工具合计 +1–2 人月。全跨度总计 ≈ **20–26 人月（约两人年）**。全跨度常态维护升至每 drop 2–4 周（压平组合的多源版本回归，靠 CI 消化）。

**覆盖率衰减预期**（待 P0 数据修正）：1.19+/1.20 源 ≈70–85%；1.16–1.18 源 ≈40–60%；1.14–1.15 源 ≈30–50%（该时代 Fabric API 不完整，Mixin 密度最高，且语料小）。

**执行策略**：P0 分析器扩展为多版本扫描（每个源版本语料 +2–3 天），用各时代覆盖率曲线决定切线（预计在 1.16.5）；扩展顺序按语料规模：1.19.2 → 1.18.2 → 1.16.5。

---

## 3.5 运行时结构设计

### 目录布局
```
mods/                  本版本模组 + centurybridge 本体 + 新版 keystone 库
cbmods/                外版本 jar 混放，由 CB 接管；版本子目录仅作指纹歧义时的手动覆盖
.centurybridge/
  cache/               转换产物，键 = 模组内容哈希 × CB 版本 × 分段数据版本（任一变化即重转）
  report/              每模组判定报告：直通 / 降级（列出被摘除的 mixin）/ 拒载（原因 + 建议），一等公民 UX
  quirks/              本地 quirk 覆盖
```

设计要点：cbmods 使意图显式化（CB 永不猜测本版本 jar 归属）；Fabric loader 对 cbmods 无视野，CB 转换后将产物注入发现流程，依赖解析横跨两目录；混版本共存靠源版本指纹路由到各自分段链；JIJ 嵌套依赖随宿主递归转换。

**已决策（P1 加载壳）**：CB 内部两块职责分开看——**运行时桥**（mixin/AW/shim 栈）别无选择必须是每版本模组；**转换器**（版本无关引擎 + 每版本数据）的注入时机走分层策略：
- 默认：**转换产物写回 mods/ + 首次重启**（cbmods 内容变化时触发，缓存键控后稳态零开销）——零配置、零 JVM 参数，兼容一切启动器与整合包分发（Modrinth/CurseForge 包无法携带 JVM 参数，agent 方案作默认等于放弃大众分发）。
- 升级：检测到 `-javaagent` 时同次启动生效（premain 转换 + 设 `fabric.addMods`）。
- 未来：Fabric loader plugin SPI 稳定后切换（勿押注，该 SPI 长期未落地）。
- 否决：改游戏 jar 本体（分发法律风险 + 与 loader 冲突）。

打包形态：**用户安装的永远只有一个文件** `centurybridge-<mcver>.jar`——内含版本无关引擎（生产期由 Python 原型移植 Java/ASM，等长替换体操消失）+ 每版本数据包（分段表/shim 清单/quirks）+ 薄的每版本 mixin 集，同一 jar 三种角色：模组（loader 加载运行时桥）/ CLI（`java -jar` 离线转换）/ agent 入口（`-javaagent:` 升级免重启）。

**转换产物存放：`cbmods/recovered/`，mods/ 保持可拷贝纯净**——用户复制 mods 列表时不得混入离开 CB 无法运行的机器产物。加载分层：
- 首选：`fabric.addMods` 指向 recovered/（agent 模式下由 premain 自动设置；服务器由 CLI 自助写入启动脚本；手动加一行 JVM 参数亦可）。
- 零配置 fallback：同步一个 JIJ 载荷 jar 进 mods/，其 fabric.mod.json 声明 `depends: centurybridge` 守卫——被无 CB 环境误拷贝时快速失败并给出可操作提示，而非神秘崩溃。
- recovered/ 同时存放 report.md 与缓存元数据；逐 jar 增量缓存（单模组变更只重转一个）。

用户操作面 = "cbmods 丢 jar + 装一个 CB"。

**语言决策：产品全线纯 Java，Python 弃用。** 单模组装不下 Python，且每 drop 的数据更新必须进 Java 构建链（CI 一条命令：下载映射 → 算分段表 → 打进 jar 资源），不能要求贡献者带 Python 环境。`analyzer/`、`cb/` 的 Python 定性为 P0 研究脚手架，使命完成（覆盖率、死亡归因、shim 优先级、管线设计验证），就地封存作参考。产品仓库两个 Java 组件：`core/`（引擎：mod/CLI/agent 三角色）+ datagen（构建期分段表生成器）。依赖 ASM（Fabric 运行时自带）+ gson（Minecraft 自带）——运行时零额外依赖。分段实现顺序从链条起点开始：1.20.1→1.20.2 先行；**intermediary 时代内部分段无需改名**（发布模组与运行时同命名空间），改名机器仅在 1.21.11→26.1 边境段启动。

### 方向性：升桥 vs 降桥

升桥（老模组→新版本）与降桥（新模组→老版本）不是镜像问题：

| | 升桥 | 降桥 |
|---|---|---|
| 本质 | 翻译调用到现存实现 | 在老引擎上实现未来 API（backport 引擎功能） |
| 成本形态 | 分段写完永久冻结 | 维护面随每个新 drop 无界增长 |
| 内容 | 新版内容是超集 | 新版内容在老版不存在，桥接层无法凭空造 |
| 数据 | DFU 同向 | 需自写反向 DFU，新特性无对应物只能丢弃 |
| 字节码 | 老 class 前向兼容 | 未来 class 版本可能被老 JVM 拒绝 |
| 需求 | 死模组无作者，桥是唯一出路 | 模组活着，生态答案是等 backport |

**决定：长程降桥列为非目标；近程降桥（N→N-1）走"元数据 + 墓碑桩"。** 永不模拟未来的 API 语义。

### 墓碑桩（tombstone stub）—— 双向通用的降级机制

缺失符号不整段拒载，而是链接期生成同签名合成桩：默认运行时抛出带清晰信息的 `CenturyBridgeMissingApiError`（惰性失败，冷路径/可选集成/环境不匹配的代码白捡），quirks 可按符号覆盖为默认值/no-op。**升桥同样适用**：未写 shim 的 L3 符号补桩降级加载，C 桶从"等 shim"变为"立即能跑、按报告逐步补"。报告列出每个墓碑桩及触发风险。

### 依赖与附属绑定策略（cbmods 内依赖闭包）

方向性直觉校准（ViaVersion 倒置的原因）：协议层适配"看见什么"（可过滤），代码层适配"存在什么"（不可过滤）；两域中容易的方向都是"固定侧为移动侧所需的超集"，只是谁固定谁移动相反。

**核心认知（先测量，不先分层）**：模组 API 边界不需要重写——① 模组类名从不混淆，附属对依赖的引用零翻译；② 签名里的 vanilla 类型被统一重写顺手改到新命名空间，与新版依赖编译出的声明自动咬合（免费组合律）；③ JVM 按符号惰性链接，兼容性是"这个附属用到的 N 个符号各自在不在"，可逐附属机械测量。**版本号是宣称，符号解析是事实；CB 用事实门代替 semver 门。**

死附属 A 依赖 L 时的决策流：

1. **默认：跨时代链接性检查**。扫 A 引用进 L 命名空间的符号集 × 新版 L 的 jar 清单（inventory.py 对任意 jar 通用）：
   - 全部解析 → 直接绑新版 L，仅放宽依赖版本范围
   - 少量缺失 → 墓碑桩降级（可选集成/冷路径白捡）
   - 承重使用损坏（extends 已删类、mixin 进 L）→ 落回第 2 步
2. **Fallback：同时代子图**。依赖也入 cbmods，整体过桥；统一重写保持子图内部 ABI 咬合；附属命运被依赖自身的过桥能力封顶。
3. **手写 API 适配器**：仅当高扇出 keystone 的机械检查大面积失败时按需立项（一层薄适配救一串附属），绝非逐依赖必需。

推论：不同时代停更的同门附属（1.20.1 与 1.21.1 的 Create 附属）可同场——各走各的 vanilla 分段链，共同对新版依赖做链接检查。

边界与规则：
- 链接成功 ≠ 行为正确——跨时代绑定在报告中标记"已链接、语义未验证"，交冒烟测试层。
- L 无新版本发布（如 Fabric 移植版停更）→ 跨时代无从谈起，仅剩同时代子图，桥接 L 本体升格为 keystone 工程；此情况可查 Modrinth 机械判定。
- 老 L 与新 L 并存 = mod id 撞车：内容型模组确定性拒载二选一；纯代码库远期可考虑 shading 共存。

**分析器跟进任务**：① 四桶判定升级为依赖闭包感知（附属有效判定 = min(自身桶位, 依赖可行性)）；② 依赖链接性检查自动化：按包名前缀归组 refs → 拉取依赖新版 jar → 逐附属出"可绑新版/需墓碑/需同时代"判定。

#### ✅ 依赖可用性扫描已完成（`data/reports/deps-availability.md`）

240 个 KPI 模组共 ~100 个外部依赖（fabric-api 内部模块已归平台层，modid≠slug 已别名归并）。**A+B+C 77.5% 经依赖封顶修正 → 有效 55.4%**（53 个模组被停更依赖封顶）。封顶 ≠ 死亡：这些正是同时代子图 fallback 的工作面，且封顶集中——porting-lib/Create Fabric 生态、trinkets、moonlight、azurelib、forgeconfigapiport 几个 keystone 解封即可批量放行。扇出榜头部 architectury/geckolib/cloth-config/REI 均活跃于 26.2，可直接跨时代绑定。另一发现：**"半停更"依赖**（patchouli/kubejs/owo 等停在 26.1.2，仅落后一个 drop）本身就是 cbmods 的近程桥接对象——CB 递归适用于依赖自身。剩余 not_found 中仍有 slug 解析漏网，55.4% 为温和下界。

## 4. 决策门

| 门 | 时点 | 判据 | 行动 |
|----|------|------|------|
| **G0 数据门** | P0 结束 | 干净链接 + 可降级 ≥ 50% | 继续 P1 |
| | | 40–50% | 收缩为"白名单精选模式"（只承诺经测试的模组列表） |
| | | < 40% | 转向做"半自动移植工具箱"（辅助人工 port 而非运行时兼容） |
| **G1** | M1 | 10 个模组中 ≥7 个可正常游玩且无存档异常 | 走公测路线 |
| **存档红线** | 任何阶段 | 发现降级路径导致世界数据损坏 | 该路径立即改为硬拒载，无例外 |

---

## 5. 风险清单

| 风险 | 等级 | 缓解 |
|------|------|------|
| Item Components 桥语义保真（NBT 时代模组的隐含假设） | 高 | 单独立项、最早动工；CI 用真实模组存档做往返测试 |
| 1.20.2–1.20.5 网络层连续重构 | 高 | 桥接到新 Fabric API 而非裸协议 |
| 渲染管线持续重构（1.21.x GPU 抽象层） | 高 | 明确划出支持范围外，拒载时给出清晰理由 |
| 语义漂移（签名存活但行为变了 → 静默 bug） | 中 | CI 语料回归 + 遥测；无法静态发现，接受长尾 |
| Mixin 长尾无法重定向 | 中 | 降级阶梯兜底；skip 遥测驱动人工规则优先级 |
| 版本节奏加快（26.x drops） | 中 | 维护成本预算内已计入；shim 分层设计降低前移成本 |
| 法律 | 低 | 运行时字节码变换、不再分发 Mojang 代码；Mojmap 许可允许开发使用 |

---

## 6. 总量结论

| 阶段 | 单人全职 |
|------|----------|
| P0 可行性量化 | 2–3 周 |
| M1 MVP | 3–4 个月 |
| M2 可信公测 | 累计 **9–12 人月** |
| 之后每年维护 | 约 2–4 人月 + quirks 社区运营 |

量级校准：与 Sinytra Connector、ViaVersion 这类由 1–3 名核心开发者长期维护的基础设施项目相当——不是周末项目，但也不需要一个团队；**一个强开发者一年，或两人半年**，可以到公测。项目真正的价值在长尾：几千个永远不会有人手动移植的小模组。

**下一步行动：P0 分析器。** 两三周、零架构风险，产出直接决定整个项目 go/no-go，且符号频率表本身就是 P1 的开发排期表。
